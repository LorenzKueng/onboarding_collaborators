# Project Memory — [Project Name]

*Project-level status log and decision record. Update at the end of each session — see the `progress_log` skill. Sister files: `CLAUDE.md` (project conventions), `AGENTS.md` (Codex conventions), `_Tasks_for_the_AI.md` (active task list).*

## Status
*Add a dated bullet at the top of each session summarising what changed. Keep older entries — they form the project's narrative log. Compress only when the section gets unwieldy (e.g. > 30 entries).*

- **YYYY-MM-DD — [short title]:** [one-paragraph summary of what was done, why, and any non-obvious decisions]

## Open TODOs
- [ ]
- [ ]

## Key Decisions
*Decisions that future-you (or a co-author) shouldn't have to re-litigate. State the decision, the date, and a one-line "why".*
-

## Known Issues / Quirks
*Bugs, dataset gotchas, replication mismatches between Stata/R/Python, environment quirks. Note workarounds if any.*
-

## People
*PI, co-authors, RAs, IT/admin contacts, vendor contacts. Format: name — affiliation — role on this project — email or other handle.*
-

## Auto-memory pointers (Claude only)
*If this project uses Claude's auto-memory, list the per-topic memory files here. Codex has no equivalent — its user context lives directly in `~/.codex/AGENTS.md`. Auto-memory location: `~/.claude/projects/<project-hash>/memory/`.*
-
